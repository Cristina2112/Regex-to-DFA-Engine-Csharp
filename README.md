# Regex-to-DFA-Engine-Csharp
A robust C# .NET 8.0 application that compiles Regular Expressions into Deterministic Finite Automata (DFA). Implements Shunting-yard parsing, NFA generation, and Subset Construction algorithms. Features console visualization of transition tables and input string validation
